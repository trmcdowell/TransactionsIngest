### Transactions Ingest

Transactions ingest is a .NET console application that uses SQLite to consolidate card transactions.

To build the project:

```
dotnet build
```

To run the project:

```
dotnet run --project TransactionsIngest
```

To run the tests:

```
dotnet test
```
